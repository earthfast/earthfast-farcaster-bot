{
  "name": "earthfast-chatbot",
  "version": "1.0.0",
  "description": "Programmaticaly deploy a static website for a token on earthfast",
  "main": "src/server.ts",
  "scripts": {
    "watch": "tsc --watch",
    "build": "rm -rf dist && tsc",
    "start": "npm run build && bun run src/server.ts",
    "generate-bot-signer": "ts-node src/generateSignerForBot.ts",
    "test": "mocha -r ts-node/register 'test/**/*.ts'",
    "test:watch": "mocha -r ts-node/register 'test/**/*.ts' --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@neynar/nodejs-sdk": "^2.0.5",
    "@types/node": "^20.9.0",
    "bun": "^1.1.38",
    "chatgpt": "^5.2.5",
    "dotenv": "^16.3.1",
    "ethers": "^6.13.4",
    "node-cron": "^3.0.3",
    "openai": "^4.76.1",
    "typescript": "^5.0.0",
    "viem": "^1.19.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node-cron": "^3.0.11",
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "ts-node": "^10.9.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
